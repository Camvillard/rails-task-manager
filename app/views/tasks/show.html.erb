<h2><%= @task.title %></h2>

<h5><%= @task.details %></h5>

<% if @task.completed %>
    <p><i class="far fa-check-square"></i> the task is all done, cool cool meuf</p>
<% else %>
    <p><i class="far fa-square"></i> the task is not completed yet</p>
<% end %>


<%= link_to "go back to the list", '/tasks' %>
